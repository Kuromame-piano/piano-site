---
import Layout from "../../layouts/Layout.astro";
import { areas } from "../../data/areas";

const pageTitle = "出張エリア一覧 | 髙橋遊月ピアノ教室";
const pageDescription =
    "髙橋遊月ピアノ教室の出張レッスン対応エリア一覧。豊洲、晴海、勝どき、月島、清澄白河、門前仲町、日本橋、森下など江東区・中央区エリアに対応。送迎不要の出張ピアノレッスンで忙しいご家庭をサポートします。";

// ItemList Schema for area index
const schema = JSON.stringify({
    "@context": "https://schema.org",
    "@graph": [
        {
            "@type": "BreadcrumbList",
            itemListElement: [
                {
                    "@type": "ListItem",
                    position: 1,
                    item: {
                        "@id": Astro.site?.toString(),
                        name: "ホーム",
                    },
                },
                {
                    "@type": "ListItem",
                    position: 2,
                    item: {
                        "@id": new URL("/area", Astro.site).toString(),
                        name: "出張エリア",
                    },
                },
            ],
        },
        {
            "@type": "ItemList",
            name: "出張ピアノレッスン対応エリア一覧",
            description: pageDescription,
            numberOfItems: areas.length,
            itemListElement: areas.map((area, index) => ({
                "@type": "ListItem",
                position: index + 1,
                item: {
                    "@type": "Service",
                    name: `${area.name}の出張ピアノ教室`,
                    url: new URL(`/area/${area.slug}`, Astro.site).toString(),
                    description: area.catchphrase,
                },
            })),
        },
    ],
});
---

<Layout title={pageTitle} description={pageDescription}>
    <!-- Structured Data -->
    <script slot="head" type="application/ld+json" set:html={schema} />
    <!-- hreflang for Japanese SEO -->
    <link
        slot="head"
        rel="alternate"
        hreflang="ja"
        href={Astro.url.toString()}
    />
    <link
        slot="head"
        rel="alternate"
        hreflang="x-default"
        href={Astro.url.toString()}
    />

    <div
        class="relative h-[40vh] flex items-center justify-center overflow-hidden bg-gradient-to-b from-[#F8F5F0] to-[#E8E0D6]"
    >
        {/* Decorative animated background circles */}
        <div class="absolute inset-0 overflow-hidden">
            <div
                class="absolute -top-20 -right-20 w-64 h-64 bg-[#A88B58]/10 rounded-full blur-3xl animate-pulse"
            >
            </div>
            <div
                class="absolute -bottom-20 -left-20 w-80 h-80 bg-[#A88B58]/8 rounded-full blur-3xl animate-[pulse_4s_ease-in-out_infinite]"
            >
            </div>
            <div
                class="absolute top-1/2 left-1/4 w-48 h-48 bg-[#A88B58]/5 rounded-full blur-2xl animate-[pulse_6s_ease-in-out_infinite]"
            >
            </div>
        </div>

        {/* Breadcrumbs (Visual) */}

        <div class="relative z-10 text-center px-6 pt-10">
            <h1
                class="text-3xl md:text-5xl font-serif mb-4 tracking-wider text-[#544A40]"
            >
                出張エリア一覧
            </h1>
            <p
                class="text-lg md:text-xl font-light text-[#A88B58] tracking-widest font-serif"
            >
                Area List
            </p>
        </div>
    </div>

    <section class="py-20 px-6 max-w-6xl mx-auto">
        <p
            class="text-center text-[#544A40]/80 mb-12 max-w-2xl mx-auto leading-relaxed"
        >
            江東区・中央区を中心に、ご自宅への出張ピアノレッスンを承っております。<br
            />
            各エリアの詳細ページでは、地域に合わせたレッスンのご案内をしています。
        </p>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {
                areas.map((area) => (
                    <a
                        href={`/area/${area.slug}`}
                        class="group block bg-white rounded-lg overflow-hidden shadow-md hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1"
                    >
                        <div class="relative h-48 overflow-hidden bg-gray-100">
                            <img
                                src={area.mainImage}
                                alt={area.mainImageAlt || area.name}
                                class="w-full h-full object-cover transition-transform duration-500 scale-110 group-hover:scale-125 opacity-80 group-hover:opacity-100"
                                loading="lazy"
                            />
                            <div class="absolute inset-0 bg-black/5 group-hover:bg-black/0 transition-colors" />
                        </div>
                        <div class="p-6">
                            <h2 class="text-xl font-serif text-[#544A40] mb-2 group-hover:text-[#A88B58] transition-colors">
                                {area.name}
                            </h2>
                            <p class="text-sm text-[#544A40]/60 mb-4 line-clamp-2">
                                {area.catchphrase}
                            </p>
                            <div class="flex flex-wrap gap-2">
                                {area.subAreas.slice(0, 3).map((sub) => (
                                    <span class="text-xs bg-[#F3EFE9] text-[#544A40] px-2 py-1 rounded">
                                        {sub}
                                    </span>
                                ))}
                                {area.subAreas.length > 3 && (
                                    <span class="text-xs bg-[#F3EFE9] text-[#544A40] px-2 py-1 rounded">
                                        +{area.subAreas.length - 3}
                                    </span>
                                )}
                            </div>
                        </div>
                    </a>
                ))
            }
        </div>
    </section>

    <section class="bg-[#F3EFE9] py-20 px-6">
        <div class="max-w-4xl mx-auto text-center">
            <h2 class="text-2xl font-serif mb-6 text-[#544A40]">
                その他のエリアについてもご相談ください
            </h2>
            <p class="mb-10 text-[#544A40]/80 leading-relaxed">
                記載のないエリアでも、近隣であれば対応可能な場合がございます。<br
                />
                まずはお気軽にお問い合わせください。
            </p>
            <a
                href="/contact"
                class="inline-block bg-[#A88B58] hover:bg-[#8A7246] text-white font-medium py-3 px-8 rounded-full transition-all duration-300 tracking-wider"
            >
                お問い合わせはこちら
            </a>
        </div>
    </section>
</Layout>
